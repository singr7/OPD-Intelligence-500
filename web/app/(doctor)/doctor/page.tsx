// Doctor console (S9). Its single job, unchanged from the S1 scaffold that
// stood here: make one patient's story scannable in 20 seconds.

import { Console } from "./_components/Console";

export const metadata = {
  title: "Doctor console — OPD Intelligence",
};

export default function DoctorPage() {
  return <Console />;
}
