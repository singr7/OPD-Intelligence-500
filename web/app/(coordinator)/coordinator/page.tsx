import { Scaffold } from "@/app/_components/Scaffold";

export default function CoordinatorPage() {
  return (
    <Scaffold
      surface="Coordinator"
      job="Keep the queue moving and run downtime without panic."
      session="Session 8"
    />
  );
}
