<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing is backed up or transferred. The care file is a cache of server rows
  the phone can re-fetch after a sign-in, and the session tokens are the one
  thing that must not ride along to a new device in a cloud backup.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
