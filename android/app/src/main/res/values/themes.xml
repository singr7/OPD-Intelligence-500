<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The window theme only has to hold the deep-clinic-green identity while
         Compose starts; everything after the first frame is OpdTheme.kt. -->
    <style name="Theme.Opd" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:navigationBarColor">@color/bg</item>
        <item name="android:windowBackground">@color/bg</item>
    </style>
</resources>
